<p id="notice"><%= notice %></p>

<div class="post-container" style="margin-top: 90px;">
  <%= render "post", post: @post %>
</div>
  

<div class="reply-container">
	<% if @post.replies.length > 0 %>
	<% @post.replies.each do |reply| %>
		<div class="jumbotron reply">
			<h4><%= link_to reply.user.username, "/users/#{reply.user.id}" %></h4>
			<p><%= linkify_hashtags(reply.body) %></p>
        <% if reply.user == current_user %> 
          <%= link_to 'Delete', "/replies/#{reply.id}", method: :delete, data: { confirm: 'Are you sure?' }, class: "link" %>
        <% end %>
		</div>
	<% end %>
<% end %>
</div>

